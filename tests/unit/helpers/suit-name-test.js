
import { suitName } from 'pitch/helpers/suit-name';
import { module, test } from 'qunit';

module('Unit | Helper | suit name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = suitName([42]);
  assert.ok(result);
});

